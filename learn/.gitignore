# learn build output (built in CI; a Terraform input) — same rule as hub/relay
/dist/
/src/generated/
node_modules/
.DS_Store

# local terraform state/lock (CI runs its own init)
.terraform/
.terraform.lock.hcl
