# Local secrets — never commit
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/

# Deploy-time copy of glucose data (build output, regenerated by CI)
site/data/

# PlatformIO build artifacts (firmware/*) — never commit .pio/
.pio/
