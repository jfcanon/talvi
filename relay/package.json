{
  "name": "talvi-relay",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-assets.mjs && esbuild src/index.js --bundle --format=esm --legal-comments=none --outfile=dist/index.js",
    "test:gate": "npm run build && node scripts/gate-test.mjs",
    "test:auth": "npm run build && node scripts/auth-test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  },
  "dependencies": {
    "@clerk/backend": "^3.16.1",
    "three": "0.185.1"
  }
}
